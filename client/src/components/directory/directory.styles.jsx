import styled from "styled-components";
import { motion } from "framer-motion";

export const DirectoryMenuContainer = styled(motion.div)`
  width: 100%;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
`;
